import 'dotenv/config';
import HundredXClient from '100x-client';
import { Environment, OrderStatus, OrderType } from '100x-client/enums';
// const MY_PRIVATE_KEY: `0x${string}` = `0x${process.env.PRI}`;
// const CONFIG = {
//   debug: false,
//   environment: Environment.MAINNET,
//   rpc: "https://rpc.blast.io",
//   subAccountId: 0,
// };
// const Client = new HundredXClient(MY_PRIVATE_KEY, CONFIG);

// console.log(Client);
// async function run() {
//   const { error, success, transactionHash } = await Client.deposit(1);

//   if (success) {
//     console.log(transactionHash);
//   }
// }

// run();
